# Production API base (Vercel rewrites /api to Render when unset locally)
# VITE_BE_PROD_URL=https://your-backend.onrender.com/api

# Local dev — axios uses http://localhost:5001/api when VITE_BE_PROD_URL is unset
# VITE_BE_PROD_URL=
